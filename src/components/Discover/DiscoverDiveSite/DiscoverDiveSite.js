import React from 'react';

const discoverDiveSite = (props) => (
    <div className="DiscoverDiveSite">
        <span>We have <b>{props.count}</b> dive sites in our DataBase! Check them out!</span>
    </div>
);


export default discoverDiveSite;